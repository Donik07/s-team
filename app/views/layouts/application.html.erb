<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Www</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

  <header class=" container d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
    <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
      <h1 id="logo"><b>S - <span id="logo-span">team</span></b></h1>
    </a>

    <ul class= "nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
      <li><%= link_to 'Главная', root_path, class: 'nav-link px-2 link-secondary fw-bold' %></li>
      <li><%= link_to 'Мои заявки', questions_path, class: 'nav-link px-2 link-secondary fw-bold ' %></li>
      <li><%= link_to 'Создать заявку',new_question_path, class: 'nav-link px-2 link-secondary fw-bold' %></li>
    </ul>
    <div class="col-md-3 text-end">
      <button type="button" class="btn btn-secondary me-2 text-white">Войти</button>
      <button type="button" class="btn btn-primary">Зарегистрироваться</button>
    </div>
  </header>

    <main class="container mt-3">
      <% flash.each do |k, v|%>
        <%= tag.div v, class: "flash alert-#{k}", role: 'alert' %>
        <%= tag.a href: "/questions", id: "close" %>
      <% end %>

      <%= yield %>
    </main>

  </body>
</html>
